# Imports

[← Back to MODULE](MODULE.md) | [← Back to INDEX](../../INDEX.md)

## Dependency Graph

```mermaid
graph TD
    src_generate[src-generate] --> analyze[analyze]
    src_generate[src-generate] --> chrono[chrono]
    src_generate[src-generate] --> emit[emit]
    src_generate[src-generate] --> std[std]
    src_generate[src-generate] --> super[super]
    src_generate[src-generate] --> types[types]
```

## Internal Dependencies

Dependencies within this module:

- `agent`
- `file_doc`
- `imports`
- `index`
- `memory`
- `module_content`
- `outline`
- `templates`

## External Dependencies

Dependencies from other modules:

- `analyze`
- `chrono`
- `emit`
- `std`
- `super`
- `types`

